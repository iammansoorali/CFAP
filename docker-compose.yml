version: '3.9'
services:
  postgres:
    image: postgres:17
    restart: unless-stopped
    environment:
      POSTGRES_USER: carbonwise
      POSTGRES_PASSWORD: carbonwise
      POSTGRES_DB: carbonwise
    ports:
      - "5432:5432"
